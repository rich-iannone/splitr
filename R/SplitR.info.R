SplitR.info <- function(mode = "add",
                        heading,
                        string
                        ){
  
  # Want to add a line after 'executables' and the row of dashes
  # the line is 'test'
#   mode <- "add"
#   heading <- "paths"
#   string <- "test"
  
}